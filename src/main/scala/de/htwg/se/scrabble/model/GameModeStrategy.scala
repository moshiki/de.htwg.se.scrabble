package de.htwg.se.scrabble.model

object GameModeStrategy{
   // var strategy = if (mode == 0) strategy1 else strategy2

    def strategy1 = println("Basic Game Mode!")
    def strategy2 = println("Secret Game Mode!")
  }